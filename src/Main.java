package pl.kul;

// import pl.kul.printer.MoviesPrinter;
// import pl.kul.repository.TsvFileBasedMoviesRepository;
// import pl.kul.repository.MoviesRepository;

import java.nio.file.Path;
import java.nio.file.Paths;
import java.util.List;

public class Main {
    public static void main(String[] args) {
        // Utworzenie repozytorium filmów
       // Path inputFilePath = Paths.get(Main.class.getResource("/title.akas.tsv.gz").getFile());
       // MoviesRepository moviesRepository = new TsvFileBasedMoviesRepository(inputFilePath);

        // Wyciągnięcie filmów z repozytorium
      //  List<Movie> allMovies = moviesRepository.findAll();

        //Wyświetlenie tego co w zadaniu
       // MoviesPrinter moviesPrinter = new MoviesPrinter(System.out);
        // moviesPrinter.print(allMovies);
    }
}
